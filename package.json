{
  "name": "ngx-statewise",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve ngx-statewise-showcase",
    "build": "npm run build:library && npm run build:showcase",
    "build:library": "ng build ngx-statewise",
    "build:showcase": "ng build ngx-statewise-showcase",
    "watch": "ng build ngx-statewise --watch --configuration development",
    "test": "npm run test:library",
    "test:library": "ng test ngx-statewise --watch=false",
    "test:coverage": "npm run test:library -- --coverage",
    "test:showcase": "ng test ngx-statewise-showcase --watch=false",
    "// --- VERIFICATION ---": "the showcase resolves ngx-statewise to dist, so its tests run once the library is built",
    "verify:docs": "node scripts/verify-docs-page.mjs",
    "check": "npm run format:check && npm run lint && npm run verify:docs && npm run test:coverage && npm run build:library && npm run test:showcase && npm run build:showcase",
    "// --- COMPATIBILITY ---": "kept out of `check`: each run installs a different Angular major from the registry",
    "verify:angular": "node tools/angular-compat/verify.mjs",
    "// --- RELEASE ---": "run from the library folder, where .release-it.json lives",
    "release:beta": "cd projects/ngx-statewise && release-it --preRelease=beta --npm.tag=beta",
    "release:stable": "cd projects/ngx-statewise && release-it --npm.tag=latest",
    "prepare": "husky",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "type": "module",
  "engines": {
    "node": "^22.22.3 || ^24.15.0 || ^26.0.0"
  },
  "private": true,
  "workspaces": [
    "projects/*"
  ],
  "dependencies": {
    "@angular/cdk": "^22.1.5",
    "@angular/common": "^22.1.5",
    "@angular/compiler": "^22.1.5",
    "@angular/core": "^22.1.5",
    "@angular/forms": "^22.1.5",
    "@angular/material": "^22.1.5",
    "@angular/platform-browser": "^22.1.5",
    "@angular/router": "^22.1.5",
    "jwt-decode": "^4.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0",
    "zone.js": "^0.16.3"
  },
  "devDependencies": {
    "@angular/build": "^22.1.7",
    "@angular/cli": "^22.1.7",
    "@angular/compiler-cli": "^22.1.5",
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "@eslint/js": "^10.0.1",
    "@release-it/conventional-changelog": "^12.0.0",
    "@vitest/coverage-v8": "^4.1.11",
    "angular-eslint": "^22.5.0",
    "eslint": "^10.10.0",
    "husky": "^9.1.7",
    "jsdom": "^30.0.1",
    "ng-packagr": "^22.1.1",
    "release-it": "^21.0.2",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.70.0",
    "vitest": "^4.0.8"
  }
}
