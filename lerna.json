{
  "version": "independent",
  "packages": ["projects/*"],
  "command": {
    "version": {
      "conventionalCommits": true,
      "allowBranch": ["main", "next"],
      "push": true
    }
  },
  "$schema": "node_modules/lerna/schemas/lerna-schema.json"
}
